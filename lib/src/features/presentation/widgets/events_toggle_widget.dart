import 'package:flutter/material.dart';

class EventsToggleWidget extends StatelessWidget {
  const EventsToggleWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [

      ],
    );
  }
}
